# Home Assistant custom-component integration tests. Pinned to the Home
# Assistant Core release this integration is verified against (2026.8.2), so
# device-automation discovery is exercised on the same code the production
# instance runs. Bridge and HA tests still run separately: the bridge's
# aiomqtt stack is installed from requirements-dev.txt.
pytest-homeassistant-custom-component==0.13.356
dataclasses-json==0.6.7
