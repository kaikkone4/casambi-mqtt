{
  "config": {
    "step": {
      "user": {
        "data": {
          "mqtt_network_name": "MQTT network name"
        }
      }
    },
    "error": {
      "invalid_network_name": "Enter one MQTT topic level without /, +, #, or null characters",
      "network_name_in_use": "This MQTT network name is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "description": "{restart_note}",
        "data": {
          "mqtt_network_name": "MQTT network name"
        }
      }
    },
    "error": {
      "invalid_network_name": "Enter one MQTT topic level without /, +, #, or null characters",
      "network_name_in_use": "This MQTT network name is already configured"
    }
  },
  "device_automation": {
    "trigger_type": {
      "press": "Button {subtype} pressed",
      "release": "Button {subtype} released",
      "hold": "Button {subtype} held",
      "release_after_hold": "Button {subtype} released after hold"
    }
  }
}
