# Test dependencies intentionally exclude casambi-bt: unit tests mock the
# hardware-specific BLE module so they run on non-Bluetooth CI runners.
dataclasses-json==0.6.7
aiomqtt==2.5.1
python-dotenv==1.2.2
pytest==9.1.1
pytest-asyncio==1.4.0
